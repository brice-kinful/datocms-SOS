import React, { Component } from "react";

const Footer = props => {
  return (
    <div id="footer">
      <div className={`wrapper full center-text`}>Footer</div>
    </div>
  );
};

export default Footer;
